<?php
namespace src;

class renderLayout {
    public function render(string $titulo, array $arquivos) {
       
        
        foreach($arquivos as $arquivo) {
            // require_once();
        }
    }
}